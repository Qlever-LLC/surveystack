import {
  deleteFileResource,
  getLabelFromKey,
  getPublicDownloadUrl,
  getSignedDownloadUrl,
  replaceLabelInKey,
  resourceTypes,
  uploadFileResource,
  uploadFileResources,
} from '@/utils/resources';

describe('uploadFileResource', () => {
  test.todo('a submission with a file resource leads to a submission with a value containing a download url');
  test.todo('a submission with a file resource with missing file data prevents a submission to be submitted');
});
describe('uploadFileResources', () => {
  //uploadFileResources
});
describe('deleteFileResource', () => {
  //uploadFileResources
});
describe('replaceLabelInKey', () => {
  //uploadFileResources
});
describe('getLabelFromKey', () => {
  //uploadFileResources
});
describe('getSignedDownloadUrl', () => {
  //uploadFileResources
});
describe('getPublicDownloadUrl', () => {
  //uploadFileResources
});
