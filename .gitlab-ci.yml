
pages:
  stage: deploy
  script:
    - git clone https://gitlab.com/our-sci/our-sci-pwa
    - mkdir .public
    - cp -r our-sci-pwa/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
