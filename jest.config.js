module.exports = {
	verbose: true,
	testMatch: ['**/?(*.)+(spec).js'],
	resetModules: true,
	resetMocks: true,
	moduleFileExtensions: ['js'],
	// preset: 'ts-jest',
	// setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
};
